# Mirac Wicked HUD 1.2.0 - minimal server.cfg example
setr ox:locale "tr"

# qbx_core has its own dependencies; keep your normal Qbox start order.
ensure ox_lib
ensure qbx_core
ensure mirac_wicked_hud

# GTAV Enhanced only: enable the rebuilt server-owned voice stack.
# The HUD does not require the deprecated "setr sv_mumble true" layer.
# voice_internal
